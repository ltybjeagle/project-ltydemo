###################################[基础概念]####################################################################
存储引擎：
    1、InnoDB：默认引擎、支持事务、支持行锁、主键索引存储数据;
    2、MyISAM：不支持事务、只支持表锁、主键索引存储数据地址;

索引：
    1、B+树结构存储索引（叶子节点存数据，数据使用链表连接）

数据存取（IO操作）：
    1、每次操作的数据量为页的整数倍（默认一页）。
    2、一页数据量默认为16k。